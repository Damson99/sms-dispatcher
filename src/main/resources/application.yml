server:
  port: 8080

web-client:
  googleapis-web-risk-base: https://webrisk.googleapis.com
  googleapis-web-risk-endpoint: /v1eap1:evaluateUri

spring:
  data:
    mongodb:
      host: localhost
      port: 27017
      database: dispatcher
      username: dispatcher_user_db
      password: dispatcher_password_db

springdoc:
  swagger-ui:
    path: /